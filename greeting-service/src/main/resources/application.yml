spring:
  application:
    name: greeting-service

  profiles:
    active:
      - local

greeting-service:
  greeting: "Hello"
  default-value: "World"

management:
  endpoints:
    web:
      exposure:
        include:
          - '*'

